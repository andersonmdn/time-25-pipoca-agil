type CustomTheme = {
  color: { val: string }
  background: { val: string }
  primary: { val: string }
  accent: { val: string }
  success: { val: string }
  warning: { val: string }
  info: { val: string }
  danger: { val: string }
  border: { val: string }
}
